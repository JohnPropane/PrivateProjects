server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/customers
    username: johnpropane
    password: password123456
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet